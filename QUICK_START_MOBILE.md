# 🚀 Быстрый старт: Xcode Simulator + Telegram Web App

## Самый простой способ (рекомендуется)

### 1. Запусти dev-сервер
```bash
npm run dev
```

### 2. Открой Xcode Simulator
```bash
open -a Simulator
```
Или через Spotlight: `Cmd+Space` → введи "Simulator"

### 3. В симуляторе открой Safari
- Найди Safari в симуляторе
- Введи адрес: `http://localhost:5173` или `http://192.168.0.124:5173`

### 4. Готово! 🎉
Приложение автоматически эмулирует Telegram WebApp API, так что все функции работают.

---

## Для полноценного тестирования в Telegram (с HTTPS)

### 1. Запусти dev-сервер
```bash
npm run dev
```

### 2. Запусти ngrok (в отдельном терминале)
```bash
ngrok http 5173
```

### 3. Скопируй HTTPS URL
Например: `https://abc123.ngrok-free.app`

### 4. Настрой Telegram бота
1. Открой [@BotFather](https://t.me/BotFather) в Telegram
2. Отправь `/newbot` и создай бота
3. Отправь `/setmenubutton`
4. Выбери своего бота
5. Введи текст кнопки: "Открыть Kalorix"
6. Введи URL: `https://abc123.ngrok-free.app` (твой ngrok URL)

### 5. Открой бота в Telegram
- Нажми на кнопку меню в боте
- Mini App откроется!

---

## Полезные команды

**Узнать локальный IP:**
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1 | awk '{print $2}' | head -1
```

**Остановить сервер:**
```bash
lsof -ti:5173 | xargs kill
```

**Открыть симулятор:**
```bash
open -a Simulator
```

---

## Что работает в мок-режиме

✅ Все UI компоненты  
✅ Роутинг и навигация  
✅ Авторизация (с мок-данными)  
✅ Форма регистрации  
✅ Эмуляция Telegram WebApp API  

Приложение автоматически определяет, запущено ли оно в Telegram или в браузере, и использует соответствующий режим.

